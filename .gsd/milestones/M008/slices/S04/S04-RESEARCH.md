# BLOCKER — auto-mode recovery failed

Unit `research-slice` for `M008/S04` failed to produce this artifact after idle recovery exhausted all retries.

**Reason**: Artifact verification failed after 3 retries for research-slice "M008/S04".

This placeholder was written by auto-mode so the pipeline can advance.
Review and replace this file before relying on downstream artifacts.